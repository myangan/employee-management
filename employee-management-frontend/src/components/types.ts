export type Employee = {
  id: number;
  firstName: string;
  lastName: string;
  title: string;
  hiredDate: string;
  address: string;
  contactNumber: string;
};
export type EmployeeData = {
  firstName: string;
  lastName: string;
};
